phpcs:
    ignoreWarnings: true
    reports:
        file:
            info: phpcs-info.txt
            full: phpcs-full.txt

phpunit:
    binary: ../../vendor/bin/phpunit.bat
    config: ../../phpunit.xml
    reports:
        file:
            log: [junit]
            testdox: [html, text]
