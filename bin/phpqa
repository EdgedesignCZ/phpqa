#!/usr/bin/env php
<?php

$options = array_slice($argv, 1);
$currentDir = realpath(__DIR__ . "/../");
$command = "cd {$currentDir} && bin/robo ci " . implode(' ', $options);
echo $command . "\n";
passthru($command);
